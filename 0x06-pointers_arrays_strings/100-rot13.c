#include "main.h"

/**
 * rot13 - encodes a string using rot 13
 * @s: the string to be encoded
 *
 * Return: the string s
 */
char *rot13(char *s);
{
	char alpha;
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{

	}
}
