#include <stdio.h>

/**
 * main - prints the arguemnts in receives
 * @argc: the number of arguements
 * @argv: array of arguements
 *
 * Return: 0 always.
 */
int main(int argc, char *argv[])
	for (i = 0; i < argc; i++)
	{
		printf("%s\n", argv[i]);
	}
	return (0);
}
