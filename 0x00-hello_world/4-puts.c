#include <stdio.h>

/**
 * main - program to display a message
 * Return:0 if program runs successfully. 1 otherwise
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
