#include <stdio.h>

/**
 * main - prints out a message using prinf function
 * Return: 0 if program runs successfully. otherwise 1
 */
int main(void)
{
	printf("with proper grammer, but the outcome is a piece of art,\n");
	return (0);
}
